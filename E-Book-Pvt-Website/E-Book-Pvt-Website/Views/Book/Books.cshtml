@model IEnumerable<E_Book_Pvt_Website.Models.Book>

@{
    ViewData["Title"] = "Books";
}

<h1>Books</h1>

<p>
    <a asp-action="AddBooks">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.book_title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.book_publisher)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.book_price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.book_ISBN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.book_author_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.book_title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.book_publisher)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.book_price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.book_ISBN)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.book_author_id)
                </td>
                <td>
                    <a asp-action="EditBooks" asp-route-id="@item.book_id">Edit</a> |
                    <a asp-action="BookDetails" asp-route-id="@item.book_id">Details</a> |
                    <a asp-action="DeleteBooks" asp-route-id="@item.book_id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
